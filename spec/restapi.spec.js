var should = require('should'),
    rally = require('..');

describe('RestApi', function () {
    var restApi = new rally.RestApi();

    beforeEach(function() {

    });

    afterEach(function() {

    });
});